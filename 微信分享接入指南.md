# 微信分享卡片接入指南

让本请帖在微信内被分享时显示「自定义卡片」（标题 + 描述 + 红金囍字缩略图）。

> ⚠️ 重要预期：卡片**只在用户从本页面点右上角「···→ 发送给朋友 / 分享到朋友圈」时生效**。
> 如果别人**复制裸链接**直接粘到对话框，仍然是纯文字链接——这是微信的规则，任何技术都改变不了。
> 页面左下角已加「分享给好友」按钮，点击会引导访客用右上角「···」分享。

---

## 一、前置条件（已满足）

- ✅ 已认证的**微信服务号**（拥有 JS-SDK 分享接口权限）
- ✅ 域名 `xjj-love-byy.cloud` 已 ICP 备案
- ✅ 代码已写好：后端签名函数 + 前端 SDK 接入

## 二、代码结构（已完成，无需改动）

| 文件 | 作用 |
|---|---|
| `node-functions/api/wx-signature.js` | EdgeOne Node 函数，给页面生成微信 `wx.config` 签名（路由 `/api/wx-signature`）|
| `public/jweixin-1.6.0.js` | 微信官方 JS-SDK，本地自托管 |
| `src/components/WxShare.tsx` | 前端：仅微信内加载 SDK、取签名、设置分享卡片，并渲染「分享给好友」按钮 |

分享卡片的标题/描述/缩略图来自 `src/config/site.ts` 的 `share` 字段，缩略图复用 `/opengraph-image` 那张红金图。

## 三、你需要操作的配置（按顺序）

### 1. 拿公众号的 AppID / AppSecret
微信公众平台 → **设置与开发 → 开发接口管理 → 基本配置**
- 记下 **AppID**
- 生成并记下 **AppSecret**（只显示一次！）

### 2. 配置「JS 接口安全域名」
微信公众平台 → **设置与开发 → 公众号设置 → 功能设置 → JS接口安全域名**
- 填写：`xjj-love-byy.cloud`（不带 https://、不带路径）
- 它会让你下载一个 `MP_verify_xxxxxxxx.txt`
- **把这个 txt 文件放到本项目的 `public/` 目录下**，提交部署后它能通过
  `https://xjj-love-byy.cloud/MP_verify_xxxxxxxx.txt` 访问，微信才校验通过
- 拿到文件后告诉我，我帮你放进 `public/`

### 3. 在 EdgeOne 控制台配置环境变量
EdgeOne Pages 项目 → **环境变量**，新增两个：
- `WX_APPID` = 你的 AppID
- `WX_APPSECRET` = 你的 AppSecret

> AppSecret 只存在服务端环境变量里，**绝不会出现在前端代码**，安全。

### 4. 在 EdgeOne 控制台绑定 KV 存储（缓存 token）
EdgeOne Pages 项目 → **KV Storage** → 新建命名空间 → 绑定到函数，
**运行时变量名填 `wx_kv`**（必须叫这个，代码里按这个名字读）。

> 作用：缓存微信 `access_token` / `jsapi_ticket`（微信对它们有每日调用次数上限，不能每次请求都去换）。
> 即使忘了配 KV，函数也能跑（只是不缓存、可能触达频次上限），但**强烈建议配**。

### 5. 把 EdgeOne 函数出口 IP 加入公众号 IP 白名单
微信公众平台 → **设置与开发 → 开发接口管理 → 基本配置 → IP白名单**
- 把 EdgeOne 函数的出口 IP 段填进去
- 出口 IP 在 EdgeOne 控制台或文档里查；若一时拿不到准确 IP，可先观察函数日志报的「invalid ip」提示里给出的 IP

## 四、部署后如何验证

1. 提交代码 → 推送 → EdgeOne 自动部署
2. 先直接访问 `https://xjj-love-byy.cloud/api/wx-signature?url=https://xjj-love-byy.cloud`
   - 正常应返回 `{"appId":"...","timestamp":"...","nonceStr":"...","signature":"..."}`
   - 若返回 `error`，按提示排查（多半是环境变量没配 / IP 白名单 / 域名）
3. 用**微信**打开 `https://xjj-love-byy.cloud`
4. 点右上角「···→ 发送给朋友」→ 应显示红金囍字卡片

### 卡片不显示时的排查
- **invalid signature**：99% 是 url 不一致。签名用的 url 必须 = 当前页面 `location.href.split('#')[0]`，代码已处理；若仍报错，检查是否被 EdgeOne 改写了 URL。
- **图片不显示**：`imgUrl` 必须是 https 绝对地址且能公网访问（当前用 `/opengraph-image`，已满足）。
- **改了文案卡片没变**：微信对分享内容有缓存，换带参数的链接（`?v=1`）强制刷新。
